#include <stdio.h>

/**
 * main - Entry point
 * Return:0
 */

int main(void)
{
	char alpha;
	char Alpha;

	for (alpha = 'a'; alpha <= 'z'; alpha++)
		putchar(alpha);
	for  (Alpha = 'A'; Alpha <= 'Z'; Alpa++)
		putchar(Alpha);

	putchar('\n');
	return (0);
}
